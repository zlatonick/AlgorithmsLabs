#pragma once
#include "globals.h"

// Generating the sorted array
int* straightArray(int size);

// Generating the reverse sorted array
int* reverseArray(int size);

// Generating the random array
int* randomArray(int size);